build:
  docker:
    web: Dockerfile
setup:
  config:
    DEBUG: 0
run:
  web: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT
release:
  image: web
  command:
    - python manage.py collectstatic --noinput
